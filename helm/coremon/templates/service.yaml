apiVersion: v1
kind: Service
metadata:
  name: {{ include "coremon.fullname" . }}
  labels:
    {{- include "coremon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coremon.selectorLabels" . | nindent 4 }}
