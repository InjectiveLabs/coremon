apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coremon.fullname" . }}-test-connection"
  labels:
    {{- include "coremon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coremon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
